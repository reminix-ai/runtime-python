[project]
name = "anthropic-agent"
version = "0.0.0"
description = "Reminix agent using the Anthropic SDK directly"
requires-python = ">=3.10"
dependencies = [
    "reminix-runtime>=0.0.21",
    "reminix-anthropic>=0.0.21",
    "anthropic>=0.75.0",
]
