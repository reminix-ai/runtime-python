[project]
name = "openai-agent"
version = "0.0.0"
description = "Reminix agent using the OpenAI SDK directly"
requires-python = ">=3.10"
dependencies = [
    "reminix-runtime>=0.0.19",
    "reminix-openai>=0.0.19",
    "openai>=2.15.0",
]
