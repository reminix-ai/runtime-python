[project]
name = "reminix-runtime-monorepo"
version = "0.0.0"
description = "Reminix Runtime - Monorepo"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"

[tool.uv.workspace]
members = ["packages/*"]
